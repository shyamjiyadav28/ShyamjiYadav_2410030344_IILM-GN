class Solution:
    def isPrime(self, n):
        # Prime numbers are greater than 1
        if n <= 1:
            return False

        # Check divisibility from 2 to √n
        for i in range(2, int(n**0.5) + 1):
            if n % i == 0:
                return False

        return True
